{
  "name": "repo-analytics-collector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Collects GitHub repository traffic/metrics and merges them into a permanent, idempotent lifetime history dataset.",
  "scripts": {
    "collect": "tsx src/collect.ts",
    "test": "node --import tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "repo-analytics": "file:.."
  }
}
